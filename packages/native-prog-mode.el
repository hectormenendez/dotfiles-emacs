;;; native-prog-mode.el --- Settings for programming modes

;;; Commentary:
;;; (this assumes you have pragmatapro font installed)

;;; Code:
(use-package prog-mode
    :config (progn
        ;; on linux this is better left alone
    )
)

(provide 'native-prog-mode)

;;; native-prog-mode.el ends here
